<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.metroui.org.ua/current/metro.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/Css/StyleProduct.css">
  <title>Detalle del Producto: <%= product.name %></title>
</head>
<body>
  <%- include('../partials/header') %>

  <div class="container py-4">
  <h1 class="text-center mb-4">Detalle del Producto</h1>
  <div class="row g-4">  <!-- g-4 para separación entre columnas -->
    <div class="col-md-6">
      <div class="card product-card">
        <img 
          src="/images/products/<%= product.image ? product.image : 'Exportarprovisorio.png' %>" 
          alt="<%= product.alt || 'Imagen de producto' %>" 
          class="card-img-top product-img"
        >
      </div>
    </div>
    <div class="col-md-6">
      <div class="card product-info-card p-4">
        <h2><%= product.name %></h2>
        <p><strong>Tipo:</strong> <%= product.productType ? product.productType.name : 'Sin tipo' %></p>
        <p><strong>Precio:</strong> $<%= product.price %></p>
        <p><strong>Descripción:</strong> <%= product.description %></p>

        <p><strong>Categorías:</strong></p>
        <% if (product.categories && product.categories.length > 0) { %>
          <ul>
            <% product.categories.forEach(category => { %>
              <li><%= category.name %></li>
            <% }) %>
          </ul>
        <% } else { %>
          <p>No tiene categorías asociadas.</p>
        <% } %>
      </div>
    </div>
  </div>
  <div class="text-center mt-4">
    <a href="/products/edit/<%= product.id %>" class="btn btn-warning">Editar Producto</a>
  </div>
</div>


  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
